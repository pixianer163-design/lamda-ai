# 阿里云 OSS 配置（示例文件）
# 复制此文件为 aliyun_oss.conf 并填入真实凭证

[aliyun]
# Access Key ID（从阿里云 RAM 控制台获取）
access_key_id = YOUR_ACCESS_KEY_ID

# Access Key Secret（敏感信息，勿提交到 Git）
access_key_secret = YOUR_ACCESS_KEY_SECRET

# OSS 端点（内网访问，流量免费）
oss_endpoint = oss-cn-beijing-internal.aliyuncs.com

# 模型存储 Bucket
oss_bucket = hktech-agent-models
models_prefix = models/

# 训练数据存储 Bucket
data_bucket = cloud-training
data_prefix = training-data/

# 本地缓存路径
local_cache_dir = /opt/hktech-agent/.oss_cache
